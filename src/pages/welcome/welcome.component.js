import template from './welcome.html';
import controller from './welcome.controller';

const component = {
  restrict: 'E',
  bindings: {

  },
  template,
  controller,
  controllerAs: 'vm',
};

export default component;
