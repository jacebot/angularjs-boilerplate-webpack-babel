import template from './button.html';
import controller from './button.controller';

const component = {
  restrict: 'E',
  bindings: {

  },
  template,
  controller,
  controllerAs: 'vm'
};

export default component;
