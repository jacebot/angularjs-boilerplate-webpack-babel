import template from './top-nav.html';
import controller from './top-nav.controller';

const component = {
  restrict: 'E',
  bindings: {

  },
  template,
  controller,
  controllerAs: 'vm',
};

export default component;
